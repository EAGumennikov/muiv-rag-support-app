# MUIV RAG Support App

Прототип системы управления знаниями службы поддержки пользователей с применением RAG-подхода.

## Что реализовано
- нормализация HTML-корпуса в Markdown
- чанкинг корпуса
- построение эмбеддингов
- построение FAISS-индекса
- извлечение retrieval-слоя по пользовательскому запросу
- построение ответа
- интеграция с Yandex AI Studio
- минимальный веб-интерфейс на Flask
- маршрут `/ask` с подключением к реальному RAG-контуру
- публичная структура сайта с 10+ страницами, хлебными крошками и каталогом статей
- просмотр статьи и скачивание markdown по `doc_id`
- SQL-слой приложения на SQLite с 10+ таблицами
- хранение обратной связи и истории запросов в SQL-базе
- контур пользователей, ролей, личных кабинетов и административного доступа
- персонализация истории вопросов, RAG-ответов и feedback по авторизованному пользователю
- расширенные страницы личного кабинета, редакторского контура и административной панели
- экспорт RAG-ответов и статей в `.docx`
- экспорт пользовательской истории, feedback, общей истории запросов и административной статистики в `.xlsx`

## Структура
- app.py - точка входа Flask-приложения
- db/ - базовый SQL-слой, конфигурация и модели данных
- services/ - сервисный слой retrieval и генерации ответа
- scripts/ - основные скрипты проекта
- templates/ - HTML-шаблоны веб-интерфейса
- static/ - статические ресурсы веб-интерфейса
- config/ - шаблоны конфигурации
- docs/ - репозиторная документация по архитектуре, RAG, SQL, ролям, экспорту и требованиям

Основной вход в документацию: [docs/index.md](/Users/eagumennikov/Projects/muiv-rag-support-app/docs/index.md).

## Зависимости
См. requirements.txt

Для экспортных сценариев добавлены:
- `python-docx` - формирование `.docx`;
- `openpyxl` - формирование `.xlsx`.

## Контуры работы

### Репозиторий
Репозиторий содержит только код проекта, шаблоны конфигурации, веб-интерфейс и техническую документацию.

### Локальный рабочий каталог
Runtime-данные, виртуальное окружение, секреты и крупные артефакты хранятся вне репозитория в отдельном рабочем каталоге.

### Целевой демонстрационный контур
Для соответствия требованиям сетевой ресурс планируется разместить на бесплатном хостинге. Локальный `workdir` используется для разработки и проверки, а размещение на хостинге будет оформлено отдельным этапом с описанием адреса, порядка развертывания и ограничений демонстрационной среды.

## Важно
Тяжелые данные, секреты, индексы и runtime-артефакты в репозиторий не включаются.

## Веб-интерфейс

На текущем этапе веб-слой включает:
- публичные страницы `/`, `/search`, `/articles`, `/glossary`, `/onboarding`, `/faq`, `/about-system`, `/about-kb`, `/testing`, `/feedback`
- хлебные крошки на всех пользовательских страницах
- маршрут `/ask` для обработки вопроса через реальный RAG-контур
- просмотр статьи `/article/<doc_id>`
- скачивание markdown `/download/<doc_id>`
- экспорт статьи в `.docx` через `/article/<doc_id>/export.docx`
- форму обратной связи

Веб-интерфейс использует существующий retrieval-контур и генерацию ответа через Yandex AI Studio по OpenAI-совместимому API.

## Роли и доступ

В приложении реализованы три авторизованные роли:
- `support_user` - рабочие пользовательские функции и личный кабинет
- `knowledge_editor` - рабочая зона редактора знаний и доступ к контентному контуру
- `admin` - полный доступ к административным разделам

Публичные страницы остаются доступными без входа.

Основные маршруты по ролям:
- публичный контур: `/`, `/search`, `/articles`, `/faq`, `/feedback` и другие открытые страницы
- авторизованный пользователь: `/cabinet`, `/cabinet/profile`, `/cabinet/history`, `/cabinet/saved-answers`, `/cabinet/feedback`, `/cabinet/help`
- редактор знаний и администратор: `/editor`, `/editor/content`, `/editor/sources`, `/editor/feedback`, `/editor/links`
- только администратор: `/admin`, `/admin/users`, `/admin/roles`, `/admin/feedback`, `/admin/feedback/<id>`, `/admin/history`, `/admin/content`, `/admin/audit`

Если пользователь авторизован, история вопросов, сформированные RAG-ответы и обращения обратной связи сохраняются с `user_id`. Публичные сценарии без входа продолжают работать, а записи остаются без привязки к пользователю.

Пользователь видит только собственные вопросы, ответы и feedback. Администратор видит общесистемную историю, обращения всех пользователей, базовую статистику и последние события аудита.

## Экспорт документов

На этапе 20 добавлены файловые сценарии без сохранения выгрузок в репозиторий. Документы формируются в памяти через `BytesIO` и сразу отдаются пользователю.

Реализованы `.docx`:
- RAG-ответ по `/export/rag-answer/<rag_answer_id>.docx`;
- статья базы знаний по `/article/<doc_id>/export.docx`.

Реализованы `.xlsx`:
- моя история запросов по `/cabinet/history/export.xlsx`;
- административная выгрузка feedback по `/admin/export/feedback.xlsx`;
- административная выгрузка общей истории запросов по `/admin/export/history.xlsx`;
- административная статистика по `/admin/export/statistics.xlsx`.

Разграничение доступа:
- статья в `.docx` доступна публично, так как сама статья уже доступна через публичный маршрут;
- пользователь выгружает только свою историю и свои RAG-ответы;
- редактор знаний может выгружать RAG-ответы для анализа источников, но не получает административные `.xlsx`;
- административные `.xlsx` доступны только роли `admin`.

## SQL-слой и SQLite

В приложении добавлен демонстрационный SQL-слой на базе `SQLAlchemy`.

Что уже хранится в базе:
- сообщения формы обратной связи;
- история поисковых запросов;
- итоговые RAG-ответы;
- связанные источники ответа;
- базовый аудит событий.
- пользователи, роли и связи доступа;
- привязка истории запросов и feedback к пользователю, если обращение выполнено после входа.

На этапе 19 добавлены nullable-связи `user_id` для `search_queries` и `feedback_messages`. Ответ RAG связан с пользователем через таблицу `search_queries`, чтобы не дублировать связь в `rag_answers`.

По умолчанию используется SQLite во внешнем `workdir`:

`sqlite:///~/Projects/muiv-rag-support-workdir/output/app/muiv_rag_support.sqlite3`

Путь можно переопределить через переменную окружения `RAG_APP_DATABASE_URL`.

Для явной инициализации таблиц можно выполнить:

```bash
cd ~/Projects/muiv-rag-support-app
source .venv/bin/activate
python3 scripts/init_app_db.py
```

Для создания демонстрационных ролей и тестовых пользователей:

```bash
cd ~/Projects/muiv-rag-support-app
source .venv/bin/activate
python3 scripts/init_demo_users.py
```

Тестовые учетные записи по умолчанию:
- `admin / admin_demo_123`
- `editor / editor_demo_123`
- `support / support_demo_123`

Значения можно переопределить через переменные окружения:
- `RAG_DEMO_ADMIN_PASSWORD`
- `RAG_DEMO_EDITOR_PASSWORD`
- `RAG_DEMO_SUPPORT_PASSWORD`

## Локальный запуск веб-приложения

Пример запуска в существующей архитектуре `repo + workdir`:

```bash
cd ~/Projects/muiv-rag-support-app
source .venv/bin/activate
pip install -r requirements.txt
python3 app.py
```

После запуска интерфейс будет доступен по адресу [http://127.0.0.1:5000](http://127.0.0.1:5000).

Если нужно сразу поднять приложение с подключением переменных окружения из workdir, можно использовать отдельный скрипт:

```bash
cd ~/Projects/muiv-rag-support-app
bash scripts/run_web.sh
```

## Соотношение repo и workdir

- `~/Projects/muiv-rag-support-app` - код, шаблоны, Flask-приложение, CLI-скрипты, документация
- `~/Projects/muiv-rag-support-workdir` - `.env`, индексы, embeddings, выходные артефакты и прочие runtime-данные

Такое разделение сохраняется и для веб-слоя: код интерфейса остается в репозитории, а рабочие данные по-прежнему должны храниться во внешнем workdir.

## Развитие проекта

На этапе 20 выполнены:
- добавлен сервис `services/export_service.py` для формирования документов и табличных выгрузок;
- реализован экспорт RAG-ответа в `.docx` с вопросом, ответом и источниками;
- реализован экспорт статьи базы знаний в `.docx` без служебных полей;
- реализована персональная выгрузка истории пользователя в `.xlsx`;
- реализованы административные выгрузки feedback, общей истории запросов и статистики в `.xlsx`;
- добавлены ссылки на экспорт в личном кабинете, на странице статьи и в административном контуре.

Выполненные проверки:
- compile-проверка измененных Python-модулей;
- smoke-проверка публичных маршрутов `/`, `/search`, `/ask`, `/feedback`, `/login`, `/articles`, `/article/<doc_id>`, `/download/<doc_id>`;
- smoke-проверка личного кабинета, редакторских и административных страниц;
- проверка запрета закрытых страниц для неавторизованного пользователя;
- проверка разграничения доступа для `support_user`, `knowledge_editor`, `admin`;
- проверка формирования `.docx` для статьи и сохраненного RAG-ответа;
- проверка формирования `.xlsx` для пользовательской истории, feedback, общей истории и административной статистики;
- проверка, что `support_user` не получает чужой RAG-ответ, а `knowledge_editor` не получает административные выгрузки.

Полноценный запуск встроенного Flask-сервера в текущей песочнице ограничен запретом на bind порта, поэтому маршруты проверялись через Flask test client с временной SQLite-БД.

Backlog дальнейшего развития:
- интерактивность `/search` и `/ask` без перезагрузки страницы через AJAX / fetch, индикатор формирования ответа, обновление ответа и источников на текущей странице, обработка ошибок retrieval и генерации в интерфейсе;
- визуальная полировка в сдержанной корпоративной стилистике: белый фон, синие акценты, темный текст, контрастные элементы, единый стиль кнопок, карточек, навигации, форм и кабинетов; официальный логотип Росатома не использовать, допустима нейтральная абстрактная пиктограмма атома как декоративный элемент;
- развитие глоссария в раздел «Отраслевой глоссарий» с категориями, расшифровками, краткими определениями и примерами использования, включая ЕОСДО, ЕОСЗ, ЕОМУ, ПСР, АРМ, ИС, СУ ИТ, ВКС, НСИ и другие отраслевые или ИТ-сокращения без чувствительной внутренней информации;
- добавление раздела «Команды первой диагностики» для Windows, Linux и macOS с назначением команд, примерами использования и типовой интерпретацией результата;
- расширение экспортов: выгрузка отдельных отчетов по ролям, фильтры по датам и экспорт подборок источников;
- подготовка схемы БД, подсчет логических строк кода, браузерная проверка и размещение на бесплатном хостинге.

Отдельная заметка по текущему SQL-слою находится в [docs/sql_layer_notes.md](/Users/eagumennikov/Projects/muiv-rag-support-app/docs/sql_layer_notes.md).
