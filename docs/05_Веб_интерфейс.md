# Веб-интерфейс и пользовательские сценарии

Web-интерфейс реализован на Flask и Jinja-шаблонах. Он включает публичный контур, личный кабинет, редакторский контур и административные страницы.

Короткое пользовательское название в интерфейсе: «Система управления знаниями поддержки». Полное название web-сервиса используется на главной странице и в проектной документации: «Система управления знаниями службы поддержки пользователей с применением RAG-подхода для поиска и формирования решений».

Интерфейс оформлен в сдержанной корпоративной стилистике: белый и очень светлый фон, синие акценты, темный текст, тонкие границы и умеренные скругления. В шапке используется нейтральная декоративная пиктограмма атома, выполненная inline SVG. Официальные логотипы и внешние брендовые изображения не используются.

## Публичный контур

Публичные страницы доступны без входа:

- `/` - главная страница;
- `/search` - поиск по базе знаний;
- `/articles` - каталог статей;
- `/article/<doc_id>` - страница статьи;
- `/download/<doc_id>` - скачивание Markdown;
- `/article/<doc_id>/export.docx` - экспорт статьи в `.docx`;
- `/glossary` - отраслевой глоссарий;
- `/diagnostics` - команды первой диагностики;
- `/onboarding` - сценарий первого использования;
- `/faq` - часто задаваемые вопросы;
- `/about-system` - описание системы;
- `/about-kb` - описание базы знаний;
- `/testing` - подходы к тестированию;
- `/feedback` - форма обратной связи;
- `/login` - вход.

## Сценарий RAG-поиска

Пользователь открывает `/search`. Страница остается публичной, но формирование ответа относится к защищенному контуру и доступно только авторизованным пользователям с ролями `support_user`, `knowledge_editor` или `admin`.

Для неавторизованного пользователя вместо активной формы показывается информационный блок с предложением войти. Это не скрывает публичный раздел, но не позволяет случайно запустить retrieval и внешний генеративный API.

Для авторизованного пользователя форма отправляет вопрос на `/ask` через AJAX / fetch без перезагрузки страницы. Интерфейс:

- блокирует кнопку на время обработки;
- показывает индикатор формирования ответа;
- выводит текст ответа на текущей странице;
- обновляет карточки источников;
- показывает ссылку на экспорт RAG-ответа в `.docx`, если ответ сохранен в SQL;
- выводит понятные ошибки авторизации, дневного лимита, пустого вопроса и сбоя обработки.

Если JavaScript отключен, HTML-форма продолжает работать через обычный POST-запрос на `/ask`. После обработки пользователь видит:

- исходный вопрос;
- ответ;
- карточки источников;
- ссылки на статьи;
- возможность скачать Markdown-материал статьи;
- возможность скачать сформированный ответ в `.docx`, если он сохранен.

Если пользователь авторизован, вопрос и ответ сохраняются с привязкой к учетной записи.

## Сценарий статьи

Каталог `/articles` показывает список документов корпуса. Страница статьи содержит:

- заголовок;
- breadcrumbs;
- текст материала;
- сведения о статье;
- скачивание Markdown;
- экспорт `.docx`;
- ссылку на оригинал при наличии.

## Сценарий адаптации сотрудника

Для поддержки адаптации нового сотрудника добавлены два публичных справочных раздела.

`/glossary` показывает отраслевые и ИТ / ИБ-сокращения. Для каждой записи выводятся термин, расшифровка, категория, комментарий и пример использования. На странице есть поиск по сокращению, расшифровке и комментарию, а также фильтр по категории.

`/diagnostics` содержит безопасный справочник команд первой диагностики для Windows, Linux, macOS и универсальных проверок. Для команды отображаются назначение, пример, что смотреть в результате и примечание. Команды оформлены моноширинным стилем.

Оба раздела используют клиентскую фильтрацию через `static/js/reference_filters.js`, не требуют отдельного API и сохраняют работоспособность публичного контура без входа.

## Сценарий обратной связи

Форма `/feedback` сохраняет обращение в SQL-БД. Если пользователь авторизован, обращение связывается с его учетной записью. Администратор видит обращения в закрытом контуре и может менять статус.

## Закрытые контуры

Закрытые страницы используют Flask-сессию и ролевые проверки:

- личный кабинет доступен авторизованным пользователям;
- редакторский контур доступен `knowledge_editor` и `admin`;
- административный контур доступен только `admin`.

Страницы закрытого контура используют единые карточки показателей, таблицы записей, быстрые ссылки и бейджи статусов feedback. Для статусов используются классы `new`, `in_progress`, `resolved`, `rejected`, при этом значения статусов в бизнес-логике не меняются.

## Навигация

Навигация разделена на:

- основное публичное меню;
- навигацию личного кабинета;
- навигацию редакторского контура;
- административную навигацию.

На пользовательских страницах используются хлебные крошки.

Шапка сайта содержит:

- нейтральную атомную пиктограмму;
- короткое название сервиса;
- подпись о RAG-поиске, базе знаний и адаптации сотрудников;
- публичное меню;
- отдельный блок входа, кабинета, редакторского и административного контура.

## Адаптивность

Карточки, таблицы, формы и навигация адаптируются под desktop, tablet и mobile. На узких экранах карточки складываются в одну колонку, кнопки растягиваются на доступную ширину, а таблицы закрытого контура переходят в компактное построчное представление с подписями полей.
